<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree View -->
    <record id="view_patient_counter_tree" model="ir.ui.view">
        <field name="name">patient.counter.tree</field>
        <field name="model">patient.counter</field>
        <field name="arch" type="xml">
            <tree string="Counters">
                <field name="name"/>
                <field name="counter_number"/>
                <field name="employee_id"/>
                <field name="waiting_count"/>
                <field name="active"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_patient_counter_form" model="ir.ui.view">
        <field name="name">patient.counter.form</field>
        <field name="model">patient.counter</field>
        <field name="arch" type="xml">
            <form string="Counter">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object" 
                                class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_button" 
                                   options='{"terminology": "archive"}'/>
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="counter_number"/>
                        </group>
                        <group>
                            <field name="employee_id"/>
                            <field name="waiting_count"/>
                            <field name="current_serving"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Queue">
                            <field name="queue_ids">
                                <tree>
                                    <field name="token_number"/>
                                    <field name="name"/>
                                    <field name="phone"/>
                                    <field name="position"/>
                                    <field name="state"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_patient_counter" model="ir.actions.act_window">
        <field name="name">Counters</field>
        <field name="res_model">patient.counter</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Menu -->
    <menuitem id="menu_patient_counter" 
              name="Counters" 
              parent="menu_patient_queue_root" 
              action="action_patient_counter" 
              sequence="20"/>
</odoo>